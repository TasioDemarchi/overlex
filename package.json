{
  "name": "overlex",
  "version": "0.9.5",
  "description": "OCR + translate overlay for Windows",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
