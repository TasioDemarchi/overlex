{
  "name": "overlex",
  "version": "0.1.0",
  "description": "OCR + translate overlay for Windows",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
